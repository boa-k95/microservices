version: 0.2
phases:
  pre_build:
    commands:
      - echo "Entered into pre-build phase"
    build:
      - mvn clean install
  post_build:
    commands:
      - echo "Build completed"
artifacts:
  files:
    - target/*
  discard-paths: yes