version: 0.2
phases:
  build:
    commands:
      - echo "build started" `date`
      - mvn test
    post_build:
      commands:
        - echo "build complete----->" `date`
        - mvn clean package
  post_build:
    commands:
      - echo "Build completed v3" `date`
artifacts:
  files:
    - target/micro-service.jar
  discard-paths: yes